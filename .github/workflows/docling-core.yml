name: Docling Core

on:
  pull_request:
    paths:
      - 'docling-core/**'
      - '.github/workflows/docling-core.yml'
  push:
    branches:
      - main
    paths:
      - 'docling-core/**'
      - '.github/workflows/docling-core.yml'

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: docling-core
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: docling-core/package-lock.json
      - run: npm ci
      - run: npm run check
